export class Lottery 
{    
    getSimpleFrequencyTable(results: string[])
    {
        for (let index = 0; index < results.length; index++) 
        {
            console.log(results[index]);
        }
    }
}